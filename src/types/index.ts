// Timeline颜色类型
export type TimelineColor = 'primary' | 'secondary' | 'muted' | 'accent' | 'destructive';

// 通用状态类型
export type Status = 'completed' | 'in-progress' | 'pending' | 'error';

// 尺寸类型
export type Size = 'sm' | 'md' | 'lg';
